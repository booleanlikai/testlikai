package com.xiaoming.Concurrency;

import com.sun.xml.internal.ws.util.CompletedFuture;

import java.io.File;
import java.util.concurrent.*;

public class FutureTest {
    public static void main(String[] args) throws ExecutionException, InterruptedException {
//        String ditectory = "C:\\Users\\likai\\Desktop";
//        String keyword = "regexp";
//        MatchCounter matchCounter = new MatchCounter(new File(ditectory), keyword);
////         = new FutureTask<>(matchCounter);
////        Thread thread = new Thread(task);
////        thread.start();
////        System.out.println(task.get());
//
//        ExecutorService executorService = Executors.newCachedThreadPool();
//        FutureTask<Integer> task = (FutureTask<Integer>) executorService.submit(matchCounter);
//        System.out.println(task.get());
//        int largestPoolSize = ((ThreadPoolExecutor) executorService).getLargestPoolSize();
//        System.out.println(largestPoolSize);
//        Exception exception=new Exception();

    }
}
